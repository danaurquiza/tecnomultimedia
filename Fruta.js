class Fruta {
  constructor(posX) {
    this.cantidadFrutas = cantidadFrutas;
    this.posX = posX;
    this.posY = posY;
    this.colorFruta=color(200, 200, 0);
 
  }

  dibujar() {    
      fill(this.miColor);
      ellipse(this.posX, this.posY, 30, 30);
    }
  

 
}
